sudo: false
language: python
python:
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install .
  - pip install codecov

script:
  - nosetests
  - coverage run nosetests

after_success:
  - codecov